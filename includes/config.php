<?php
$BASE = '/trynorthwest';
$current = isset($current) ? $current : '';
$page_title = isset($page_title) ? $page_title : 'NorthWest Construction Control';
$page_desc = isset($page_desc) ? $page_desc : 'NorthWest Construction Control provides streamlined draw inspections and support services for construction lenders nationwide.';
$body_id = isset($body_id) ? $body_id : 'body_page';
$body_class = isset($body_class) ? $body_class : 'body_page';
?>
